uv init : It creates the pyproject.toml
uv add : It builds your Virtual Environment (.venv) automatically. You don't have to manually create or "activate" it like the old days; uv handles the isolation for you.
uv sync
source .venv/bin/activate
git init: You want to commit your pyproject.toml and uv.lock right away so your project "DNA" is saved

