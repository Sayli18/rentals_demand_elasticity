"""Phase 4 — causal elasticity estimation.

Runs Double ML for the three estimands (beta_base, beta_discount, beta_cancel),
plus a Causal Forest pass for heterogeneity. Validates each estimate against the
DGP-truth elasticities and writes results to reports/causal_elasticity/.

To be implemented.
"""
